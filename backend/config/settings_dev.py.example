DEBUG = True

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'logistika',
        'USER': 'postgres',
        'PASSWORD': '',
        'HOST': 'localhost',
        'PORT': 5432,
    }
}

# Local dev: SMS va mock to'lov
SMS_VERIFICATION_REQUIRED = False
PAYMENTS_ALLOW_MOCK = True
SUBSCRIPTIONS_ENFORCED = False

# Eskiz SMS
ESKIZ_EMAIL = ''
ESKIZ_PASSWORD = ''

# Maps
GOOGLE_MAPS_API_KEY = ''
YANDEX_ROUTING_API_KEY = ''

# Email (ixtiyoriy)
EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
